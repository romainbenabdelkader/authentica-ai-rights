{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://lockdna.tech/schemas/authentica-ai-rights/manifest-v1.schema.json",
  "title": "AUTHENTICA AI Rights Manifest (v1)",
  "description": "Machine-readable manifest describing origin, human/AI status, and AI-usage restrictions for creative works registered via AUTHENTICA.",
  "type": "object",
  "required": [
    "@context",
    "type",
    "id",
    "uid_auth",
    "name",
    "creator",
    "origin",
    "rightsAI",
    "fingerprint",
    "published",
    "proofSince",
    "signature"
  ],
  "properties": {
    "@context": {
      "description": "JSON-LD contexts used by the manifest.",
      "type": ["string", "array"],
      "items": {
        "type": "string"
      }
    },
    "type": {
      "description": "Type of the described resource.",
      "type": "string",
      "enum": ["CreativeWork"]
    },
    "id": {
      "description": "Stable URI of the manifest instance.",
      "type": "string",
      "format": "uri"
    },
    "uid_auth": {
      "description": "AUTHENTICA native identifier (UID_AUTH) of the work.",
      "type": "string"
    },
    "name": {
      "description": "Title of the work.",
      "type": "string"
    },
    "creator": {
      "description": "List of creators and their roles.",
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["id", "role"],
        "properties": {
          "id": {
            "description": "Identifier of the creator (URI or URN).",
            "type": "string"
          },
          "role": {
            "description": "Roles played by this creator (author, composer, director, etc.).",
            "type": "array",
            "items": {
              "type": "string"
            },
            "minItems": 1
          }
        },
        "additionalProperties": false
      }
    },
    "origin": {
      "description": "Information about the human or AI origin of the work.",
      "type": "object",
      "required": ["human", "ai_assisted"],
      "properties": {
        "human": {
          "description": "True if the work is of human origin.",
          "type": "boolean"
        },
        "ai_assisted": {
          "description": "True if AI tools assisted the creation process.",
          "type": "boolean"
        },
        "noteOfOrigin": {
          "description": "Optional reference (hash, URI) to a cryptographic Note of Origin or external proof.",
          "type": ["string", "null"]
        }
      },
      "additionalProperties": false
    },
    "rightsAI": {
      "description": "Explicit AI-usage rights and restrictions.",
      "type": "object",
      "required": [
        "training",
        "analysis",
        "generation",
        "remix",
        "commercialUseByAI",
        "exceptionTDM_EU"
      ],
      "properties": {
        "training": {
          "description": "Permission for use of the work to train AI models.",
          "type": "string",
          "enum": ["allowed", "forbidden", "restricted"]
        },
        "analysis": {
          "description": "Permission for non-training analytical processing by AI.",
          "type": "string",
          "enum": ["allowed", "forbidden", "restricted"]
        },
        "generation": {
          "description": "Permission for AI-generated continuations/variations based on this work.",
          "type": "string",
          "enum": ["allowed", "forbidden", "restricted"]
        },
        "remix": {
          "description": "Permission for AI to remix or transform this work.",
          "type": "string",
          "enum": ["allowed", "forbidden", "restricted"]
        },
        "commercialUseByAI": {
          "description": "Permission for AI providers to exploit the work commercially.",
          "type": "string",
          "enum": ["allowed", "forbidden", "restricted"]
        },
        "exceptionTDM_EU": {
          "description": "Reservation of rights under EU Text and Data Mining (TDM) exception (Directive 2019/790).",
          "type": "string",
          "enum": ["reserved", "not-reserved"]
        }
      },
      "additionalProperties": false
    },
    "fingerprint": {
      "description": "Hashes or fingerprints for different media types of the work.",
      "type": "object",
      "properties": {
        "audio": {
          "description": "Hash or fingerprint of the audio representation (e.g. sha256:...).",
          "type": ["string", "null"]
        },
        "image": {
          "description": "Hash or fingerprint of the image representation, if any.",
          "type": ["string", "null"]
        },
        "text": {
          "description": "Hash or fingerprint of the text representation, if any.",
          "type": ["string", "null"]
        }
      },
      "additionalProperties": false
    },
    "published": {
      "description": "ISO 8601 timestamp when the manifest was first published.",
      "type": "string",
      "format": "date-time"
    },
    "proofSince": {
      "description": "ISO 8601 timestamp from which cryptographic proof of existence is claimed.",
      "type": "string",
      "format": "date-time"
    },
    "signature": {
      "description": "Detached digital signature of the manifest (e.g. ed25519:...).",
      "type": "string"
    }
  },
  "additionalProperties": false
}